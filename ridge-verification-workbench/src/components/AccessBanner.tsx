export function AccessBanner() {
  return (
    <div className="rounded-2xl border border-ridge-amber/30 bg-ridge-amber/10 p-4 text-sm leading-6 text-ridge-amber">
      <strong className="font-semibold text-ridge-amber">Access layer placeholder:</strong> v1 is a front-end-only demo with local mock data. Authentication, payment gating, policy-backed workspaces, and backend APIs are intentionally not active.
    </div>
  );
}
